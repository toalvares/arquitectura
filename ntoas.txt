<!-- // Validación del formulario de inicio de sesión
if ($_SERVER["REQUEST_METHOD"] == "POST") {
  // Obtener los datos del formulario
  $rut = $_POST['rut'];
  $password = $_POST['contraseña'];

  // Escapar caracteres especiales para evitar ataques de inyección SQL
  $rut = $conn->real_escape_string($rut);
  $password = $conn->real_escape_string($password);

  // Consulta para verificar si el usuario existe en la base de datos
  $sql = "SELECT * FROM usuarios WHERE rut = '$rut' AND contraseña = '$password'";
  
  $result = $conn->query($sql);

  // Verificar si se encontró un resultado
  if ($result->num_rows == 1) {
    // El inicio de sesión fue exitoso
    echo '<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>';
    echo '<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>';
    echo '<script>
            $(document).ready(function() {
                $("#log").modal("show");
                setTimeout( function() { window.location.href = "index.php"; }, 2000 );
             });
        </script>';
  } else {
    echo '<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>';
    echo '<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>';

    echo '<script>
            $(document).ready(function() {
              $("#msgerror").html("Rut o contraseñas incorrectos")});</script>';
    
  }
}

$conn->close();
?>
-->